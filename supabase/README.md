# Supabase backend scaffold

This folder stores SQL migrations and seed data for the quiz tracker schema.

## Expected workflow

1. Create a Supabase project.
2. Install the Supabase CLI locally.
3. Link your local project to the remote Supabase project.
4. Apply migrations from `migrations/`.

The initial schema is in `migrations/202605010001_initial_schema.sql`.
